{
  "name": "@opto-sync-test/trpc-typescript-fullstack-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:opto-sync": "npm --prefix vendor/opto-sync-clients/clients/ts ci && npm --prefix vendor/opto-sync-clients/clients/ts run build",
    "build:web": "esbuild web/page.ts web/sw.ts --bundle --format=esm --platform=browser --target=es2022 --outdir=.build/web",
    "build": "npm run build:opto-sync && npm run build:web && tsc --noEmit",
    "test": "node --test --import tsx test/*.test.ts",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@opto-sync/client": "file:vendor/opto-sync-clients/clients/ts",
    "@trpc/client": "^11.4.3",
    "@trpc/server": "^11.4.3",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "esbuild": "^0.25.10",
    "fake-indexeddb": "^6.2.2",
    "playwright": "^1.56.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
